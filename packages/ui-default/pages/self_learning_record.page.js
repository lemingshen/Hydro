/*
 * PTA fork: retired. The record-page tutor chat (a legacy surface reached
 * through /record/:rid?slssid=…&slpid=…) is no longer linked from anywhere:
 * programming tasks are tutored inside the Scratchpad with line-anchored
 * cards and the launcher panel. This stub remains only so that extracting
 * the zip over an older tree cleanly disables the previous version.
 */
export default {};
